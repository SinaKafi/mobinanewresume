// "use client";

// import { ChakraProvider } from "@chakra-ui/react";
// import { customSystem } from "./theme";

// export default function ChakraProviders(props: { children: React.ReactNode }) {
//   return <ChakraProvider value={customSystem}>{props.children}</ChakraProvider>;
// }
